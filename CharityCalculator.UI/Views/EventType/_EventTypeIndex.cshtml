@model IEnumerable<CharityCalculator.UI.Models.EventTypeVM>

@{
    ViewData["Title"] = "Index";
}

<h1>Event Types</h1>

<p>
    <a data-ajax="true" data-ajax-success="getEventTypeCreatePage" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SupplementInPercentage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaximumDonationAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SupplementInPercentage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaximumDonationAmount)
            </td>
            <td>
                <form data-ajax="true" data-ajax-success="getEventTypeEditPage" asp-action="Edit" asp-route-id="@item.Id" method="get">
                    <button type="submit" class="btn btn-link">
                        Edit
                    </button>
                </form>
                <form data-ajax="true" data-ajax-success="getEventTypeDetailPage" asp-action="Details" asp-route-id="@item.Id" method="get">
                    <button type="submit" class="btn btn-link">
                        Details
                    </button>
                </form>
                <form data-ajax="true" data-ajax-success="getEventTypeIndex" asp-action="Delete" asp-route-id="@item.Id" method="post">
                    <button type="submit" class="btn btn-link" onclick="return confirm('Are you sure you want to delete this record?')">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
